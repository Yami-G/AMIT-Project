import 'package:flutter/material.dart';

class SecandScreen extends StatelessWidget {
  const SecandScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('hi'),
    );
  }
}
